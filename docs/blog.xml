<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Ankur Garg</title>
<link>https://agarg-dev.github.io/blog.html</link>
<atom:link href="https://agarg-dev.github.io/blog.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.8.26</generator>
<lastBuildDate>Thu, 11 Jun 2026 06:00:00 GMT</lastBuildDate>
<item>
  <title>CLVQ-VAE: Discovering Discrete Concepts Across Transformer Layers</title>
  <link>https://agarg-dev.github.io/posts/clvqvae/</link>
  <description><![CDATA[ 





<div class="callout callout-style-simple callout-note">
<div class="callout-body d-flex">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-body-container">
<p>This post accompanies our paper <em>Cross-Layer Discrete Concept Discovery for Interpreting Language Models</em> (with Xuemin Yu, Hassan Sajjad, and Samira Ebrahimi Kahou). <a href="https://arxiv.org/abs/2506.20040">📄 Paper</a> · <a href="https://github.com/agarg-dev/CLVQVAE">💻 Code</a></p>
</div>
</div>
</div>
<section id="why-is-it-so-hard-to-look-inside-a-transformer" class="level2">
<h2 class="anchored" data-anchor-id="why-is-it-so-hard-to-look-inside-a-transformer">Why is it so hard to look inside a transformer?</h2>
<p>Large language models work remarkably well, but <em>how</em> they work remains opaque. A natural first instinct is to pick a layer, look at its activations, and try to assign meaning to individual neurons or directions. Unfortunately, the transformer architecture itself fights you on this.</p>
<p>The culprit is the <strong>residual stream</strong>. Every layer <em>adds</em> its contribution to a running representation, which means:</p>
<ol type="1">
<li><strong>Features get duplicated.</strong> A feature written at layer 3 is still visible at layers 8, 10, and 12, so a single-layer analysis sees the same concept many times over.</li>
<li><strong>Features get mixed.</strong> The stream linearly superimposes many features on top of each other, so any single snapshot is a tangled sum of everything written so far.</li>
</ol>
<p>Sparse autoencoders (SAEs) address the mixing problem, and recent cross-layer variants attack the duplication too. But SAEs operate in <em>continuous</em> space, where concepts split across many neurons without clear boundaries, and isolating one requires arbitrary activation thresholds or combinations of multiple feature directions. Humans, meanwhile, reason in discrete categories: a token either expresses <em>negation</em> or it doesn't. This mismatch limits interpretability.</p>
</section>
<section id="our-idea-quantize-the-transformation-between-layers" class="level2">
<h2 class="anchored" data-anchor-id="our-idea-quantize-the-transformation-between-layers">Our idea: quantize the <em>transformation</em> between layers</h2>
<p>Instead of asking "what is encoded at layer <img src="https://latex.codecogs.com/png.latex?l">?", we ask:</p>
<blockquote class="blockquote">
<p><strong>What concepts are needed to explain how the representation at layer <img src="https://latex.codecogs.com/png.latex?l"> becomes the representation at layer <img src="https://latex.codecogs.com/png.latex?h">?</strong></p>
</blockquote>
<p><strong>CLVQ-VAE</strong> (Cross-Layer Vector Quantized Variational Autoencoder) answers this by acting as a <em>transcoder with a discrete bottleneck</em>. It takes activations from a lower layer, forces them through a finite codebook of vectors, and reconstructs the activations of a higher layer. Whatever survives the bottleneck, the codebook vectors that get used, are by construction the concepts essential to the cross-layer transformation. Duplicated residual-stream features collapse onto a single codebook entry instead of showing up over and over, and unlike SAEs, each input maps to <em>one</em> discrete concept vector rather than a linear combination of active neurons.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://agarg-dev.github.io/images/clvq_vae.png" class="img-fluid figure-img" style="width:85.0%"></p>
<figcaption>Overview of the CLVQ-VAE framework: lower-layer activations pass through an adaptive residual encoder, get discretized into concept vectors by the quantizer, and a transformer decoder reconstructs the higher-layer representation.</figcaption>
</figure>
</div>
<p>The framework has three crucial components.</p>
<section id="an-adaptive-residual-encoder" class="level3">
<h3 class="anchored" data-anchor-id="an-adaptive-residual-encoder">1. An adaptive residual encoder</h3>
<p>Pre-trained LM embeddings are already information-rich; a randomly initialized encoder that fully transforms them destroys that structure before training can recover it. But a frozen identity map limits what can be discovered. We interpolate between the two with a <em>learnable</em> mixing coefficient:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cmathbf%7Bz%7D_e%20=%20(1%20-%20%5Calpha)%5C,%5Cmathbf%7Bx%7D%20+%20%5Calpha%20%5Ccdot%20%5Cmathrm%7BLN%7D(W%5Cmathbf%7Bx%7D%20+%20%5Cmathbf%7Bb%7D),%20%5Cqquad%20%5Calpha%20=%20%5Csigma(a)%20%5Ccdot%200.5%0A"></p>
<p>Capping <img src="https://latex.codecogs.com/png.latex?%5Calpha"> at 0.5 prevents the encoder from drifting too far from the original embedding. In practice, <img src="https://latex.codecogs.com/png.latex?%5Calpha"> behaves like a built-in curriculum: it starts small (preserving the input) and grows as the encoder learns what is safe to change. Letting <img src="https://latex.codecogs.com/png.latex?%5Calpha"> range all the way to 1 measurably hurt codebook utilization.</p>
</section>
<section id="a-vector-quantizer-with-stochastic-top-k-sampling" class="level3">
<h3 class="anchored" data-anchor-id="a-vector-quantizer-with-stochastic-top-k-sampling">2. A vector quantizer with stochastic top-k sampling</h3>
<p>The classic VQ-VAE failure mode is <strong>codebook collapse</strong>, where a few vectors get all the assignments and the rest die. Rather than deterministically snapping each encoder output to its nearest codebook vector, we sample from the <img src="https://latex.codecogs.com/png.latex?k=5"> nearest entries with probabilities <img src="https://latex.codecogs.com/png.latex?%5Cpropto%20%5Cexp(-d/%5Ctau)"> at <img src="https://latex.codecogs.com/png.latex?%5Ctau%20=%201.0">. This controlled exploration keeps more of the codebook alive and improves concept diversity. Codebook updates use exponential moving averages rather than backpropagation, which keeps training stable despite the discrete assignments.</p>
<p>For codebook <strong>initialization</strong>, we compare random sampling against two k-means variants: default (Euclidean) k-means, and a spherical variant that clusters unit-normalized embeddings by direction and then rescales each centroid by the average magnitude of its cluster. Which one is "better" turns out to be a genuinely interesting question, and there is more on that below.</p>
</section>
<section id="a-transformer-decoder" class="level3">
<h3 class="anchored" data-anchor-id="a-transformer-decoder">3. A transformer decoder</h3>
<p>A 6-layer transformer decoder maps the quantized concepts back to higher-layer activations. Because it reconstructs the whole sequence at once rather than generating autoregressively, self-attention is fully bidirectional, with no causal mask needed. Cross-attention to the unquantized encoder outputs acts as a residual path around the bottleneck, offloading low-level reconstruction details so the codebook can focus on distinct, high-level concepts. Our ablations confirm this: with cross-attention, codebook vectors are more orthogonal in 8 of 9 configurations and concepts are more faithful in 7 of 9.</p>
</section>
</section>
<section id="does-it-find-concepts-that-actually-matter" class="level2">
<h2 class="anchored" data-anchor-id="does-it-find-concepts-that-actually-matter">Does it find concepts that actually matter?</h2>
<p>We evaluate across four models (fine-tuned RoBERTa and BERT, plus zero-shot LLaMA-2-7B and Qwen2.5-3B-Instruct) on three datasets (ERASER-Movie sentiment, Jigsaw toxicity, AG News), giving twelve model and dataset configurations. Baselines are latent-concept clustering, a single-layer VQ-VAE, and a cross-layer SAE.</p>
<p>The main quantitative test is a perturbation study: identify each sentence's most salient token via Layer Integrated Gradients, find the concept it maps to, remove that direction from the sentence representation via orthogonal projection, and measure how much a probe's accuracy drops. The bigger the drop, the more faithful the concept. Two controls guard against artifacts: removing a <em>random</em> direction barely moves accuracy, and removing a <em>different</em> active codebook vector causes far smaller drops in 8 of 9 tested configurations, so the damage is specific to the identified concept, not to perturbation in general.</p>
<p>The headline result: on RoBERTa/ERASER-Movie, removing the single concept CLVQ-VAE identifies sends probe accuracy from 0.878 down to 0.059, a <strong>93.2% drop</strong>. Across all twelve configurations, CLVQ-VAE achieves the lowest perturbed accuracy in five, and a VQ-VAE method (cross-layer or single-layer) ranks first or second in <em>all twelve</em>, supporting the discrete-bottleneck paradigm itself.</p>
<p>The most striking comparative finding involves SAEs: they show near-zero or even negative drops on <strong>all six decoder-model settings</strong>, and CLVQ-VAE beats SAE in 10 of 12 configurations, by an average of 15.8 percentage points on the decoder models. We replicated the decoder failure with an independent SAE implementation (a Llama Scope-style TopK transcoder), suggesting this reflects a genuine limitation of sparse continuous representations for single-vector concept ablation, consistent with the feature-splitting hypothesis, and motivating the discrete bottleneck.</p>
</section>
<section id="are-the-concepts-real-concepts-or-just-compression" class="level2">
<h2 class="anchored" data-anchor-id="are-the-concepts-real-concepts-or-just-compression">Are the concepts real concepts, or just compression?</h2>
<p>A skeptic might worry the codebook is merely compressing sentences rather than encoding distinct concepts. Three analyses say otherwise:</p>
<ul>
<li><strong>Vector level:</strong> label purity well above the random baseline (0.60 to 0.76 vs. 0.50 on binary tasks), with a visible spike of vectors firing <em>exclusively</em> on one class.</li>
<li><strong>Token level:</strong> 28 to 76% of content tokens route to <em>different</em> codebook vectors depending on context. My favorite example: in ERASER-Movie, the word <em>entertainment</em> routes to vector #137 (91% negative purity) in "does not serve as... comic entertainment" but to vector #73 (97% positive purity) in "over-the-top entertainment." Same surface form, different concept, exactly what concept-level (rather than word-level) encoding should look like.</li>
<li><strong>Sentence level:</strong> lexically similar sentences with the <em>same</em> label share 2.1 to 7.6x more codebook vectors than equally similar sentences with <em>different</em> labels, up to 30x at the strictest lexical-similarity threshold.</li>
</ul>
</section>
<section id="do-judges-human-and-llm-agree" class="level2">
<h2 class="anchored" data-anchor-id="do-judges-human-and-llm-agree">Do judges, human and LLM, agree?</h2>
<p>Faithfulness tells you a concept influences the model; it doesn't tell you the concept is <em>understandable</em>. So we evaluated interpretability two ways.</p>
<p><strong>LLM-as-a-judge.</strong> An ensemble of four LLM judges (GPT-4o-mini, Claude 3.5 Haiku, Gemini 2.0 Flash, Gemini 2.0 Flash Lite) rated how plausibly each method's concept explains the model's prediction, across stratified samples from all configurations. CLVQ-VAE ranks first with a 66.7% win rate, mean rating 1.890, and MRR 0.611, ahead of single-layer VQ-VAE, SAE, and clustering (44.4% win rate each).</p>
<p><strong>Human evaluation.</strong> 14 annotators looked at word clouds of the tokens mapped to a sentence's most salient concept, without seeing the sentence, the model's prediction, or the label, and tried to predict what the model said.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Metric</th>
<th>Clustering</th>
<th>CLVQ-VAE</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Fleiss' kappa (inter-annotator agreement)</td>
<td>0.59</td>
<td><strong>0.864</strong></td>
</tr>
<tr class="even">
<td>Avg. self-reported confidence (1 to 10)</td>
<td>5.98</td>
<td><strong>8.44</strong></td>
</tr>
<tr class="odd">
<td>Model alignment rate</td>
<td>54.1%</td>
<td><strong>78.2%</strong></td>
</tr>
</tbody>
</table>
<p>Annotators interpreting CLVQ-VAE visualizations agreed with each other at an "almost perfect" level, felt substantially more confident, and matched the model's actual prediction 24 points more often.</p>
<p>My favorite qualitative finding came from the <em>failure</em> cases. In one false negative, a positive review praised actors for a "dead-on mockery" and "decent imitation", and the salient concept lit up with <em>lifted, fake, parody, imitation, ripped</em>. The model had bound imitation-words to negative sentiment and couldn't distinguish criticism of unoriginality from praise of a good impersonation. That's exactly the kind of mechanistic insight into model errors that interpretability tools should surface.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://agarg-dev.github.io/images/FN.png" class="img-fluid figure-img" style="width:60.0%"></p>
<figcaption>A false negative: the model misreads a positive review of a "decent imitation" as negative. The salient concept's word cloud (parody, fake, lifted, imitation, ripped) reveals that the model bound imitation-words to negative sentiment.</figcaption>
</figure>
</div>
<p>The method is just as legible when the model is <em>right</em>. For a correctly predicted negative review, the salient concept collects unambiguous markers of absence and negation (nothing, barely, none, whatsoever, hardly, neither), a coherent "deficiency" concept that maps cleanly onto the model's call.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://agarg-dev.github.io/images/TN.png" class="img-fluid figure-img" style="width:60.0%"></p>
<figcaption>A true negative: the salient concept for a correctly predicted negative review clusters negation and absence words such as nothing, barely, none, whatsoever, and hardly.</figcaption>
</figure>
</div>
</section>
<section id="a-trade-off-worth-knowing-initialization-depends-on-your-goal" class="level2">
<h2 class="anchored" data-anchor-id="a-trade-off-worth-knowing-initialization-depends-on-your-goal">A trade-off worth knowing: initialization depends on your goal</h2>
<p>One finding I find genuinely instructive: <strong>which codebook initialization is "best" depends on which evaluation you ask.</strong></p>
<p>Under the <em>faithfulness</em> metric, default Euclidean k-means achieves the lowest perturbed accuracy in 7 of 12 configurations, often beating the spherical variant. But under the <em>LLM-judge</em> evaluation of semantic coherence, the ranking flips: spherical wins 62.5% of configurations with the highest mean rating and lowest variance, ahead of k-means (58.3%) and far ahead of random (25%), with strong inter-judge agreement (Kendall's W = 0.793).</p>
<p>In other words, k-means seems to find directions that are <em>functionally</em> important to the model's decisions, while clustering by direction (spherical) yields concepts that <em>humans and LLMs find more coherent</em>. Both k-means variants beat random initialization, which adds variance from its unstructured starting codebook. If you use CLVQ-VAE, pick the initialization to match your goal: functional faithfulness or semantic interpretability.</p>
</section>
<section id="try-it" class="level2">
<h2 class="anchored" data-anchor-id="try-it">Try it</h2>
<p>The code, scripts, and prepared datasets are available on <a href="https://github.com/agarg-dev/CLVQVAE">GitHub</a>. The pipeline supports RoBERTa, BERT, LLaMA-2-7B, and Qwen2.5-3B-Instruct:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> clone https://github.com/agarg-dev/CLVQVAE.git</span>
<span id="cb1-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">pip</span> install torch transformers neurox scikit-learn numpy pandas matplotlib seaborn wordcloud</span>
<span id="cb1-3"></span>
<span id="cb1-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Train (configure dataset, layer pair, codebook size, initialization in scripts/main.sh)</span></span>
<span id="cb1-5"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">bash</span> scripts/main.sh</span>
<span id="cb1-6"></span>
<span id="cb1-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Faithfulness evaluation</span></span>
<span id="cb1-8"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">bash</span> scripts/faithfulness.sh</span>
<span id="cb1-9"></span>
<span id="cb1-10"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># LLM-as-a-judge evaluation and inter-judge agreement</span></span>
<span id="cb1-11"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">bash</span> scripts/llm_judge_evaluation.sh</span>
<span id="cb1-12"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">bash</span> scripts/analyze_agreement.sh</span></code></pre></div></div>
<p>Layer pairs used in the paper: 8 to 12 for BERT/RoBERTa, 28 to 32 for LLaMA-2-7B, 32 to 36 for Qwen2.5-3B.</p>
</section>
<section id="limitations-and-whats-next" class="level2">
<h2 class="anchored" data-anchor-id="limitations-and-whats-next">Limitations and what's next</h2>
<p>A few honest caveats. Extracting paired activations from multiple layers is memory-hungry, and k-means initialization adds cost that scales with dataset and codebook size, so models beyond ~70B parameters would need further optimization. The perturbation-based faithfulness metric differentiates methods well but is insensitive to hyperparameter changes like temperature, top-k, or codebook size, and the LLM-judge evaluation is sensitive to prompt construction. Finally, the framework trains a separate model per layer pair, and for decoder-only models we use mean-pooled embeddings in place of a CLS token, which may encode somewhat different information.</p>
<p>Longer term, I'm excited about discrete cross-layer concepts as a building block: tracking how a concept vocabulary evolves across <em>many</em> layer pairs, applying the framework to instruction-tuned and reasoning models, and using discovered concepts for targeted interventions like steering and debiasing.</p>
</section>
<section id="citation" class="level2">
<h2 class="anchored" data-anchor-id="citation">Citation</h2>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode bibtex code-with-copy"><code class="sourceCode bibtex"><span id="cb2-1"><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">@article</span>{<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">garg2026clvqvae</span>,</span>
<span id="cb2-2">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">title</span>   = {Cross-Layer Discrete Concept Discovery for Interpreting Language Models},</span>
<span id="cb2-3">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">author</span>  = {Garg, Ankur and Yu, Xuemin and Sajjad, Hassan and Ebrahimi Kahou, Samira},</span>
<span id="cb2-4">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">journal</span> = {arXiv preprint arXiv:2506.20040},</span>
<span id="cb2-5">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">year</span>    = {2026}</span>
<span id="cb2-6">}</span></code></pre></div></div>


</section>

 ]]></description>
  <category>interpretability</category>
  <category>LLMs</category>
  <category>research</category>
  <guid>https://agarg-dev.github.io/posts/clvqvae/</guid>
  <pubDate>Thu, 11 Jun 2026 06:00:00 GMT</pubDate>
  <media:content url="https://agarg-dev.github.io/images/clvq_vae.png" medium="image" type="image/png" height="55" width="144"/>
</item>
</channel>
</rss>
